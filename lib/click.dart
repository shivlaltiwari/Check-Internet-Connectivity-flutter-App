
import 'package:flutter/material.dart';

class ClickedMe extends StatelessWidget {
  const ClickedMe({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("from home page"),
      ),
    );
  }
}
